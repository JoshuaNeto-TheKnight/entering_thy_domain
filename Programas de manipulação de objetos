#Parte das classes
class Conta_Corrente:
  def __init__(self, numero, saldo, nome_titular):
    self.__numero = numero
    self.__saldo = saldo
    self.__nome_titular = nome_titular
  @property
  def saldo(self):
    return self.__saldo
  @saldo.setter
  def saldo(self, valor):
    self.saldo = valor
  @property
  def numero(self):
    return self.__numero
  @numero.setter
  def numero(self, valor):
    self.numero = valor
class Banco:
  def __init__(self):
    self.__banco = []
  def listar_contas(self, conta):
    self.__banco.append(conta)
  def soma_de_saldos(self):
    soma = 0
    for i in range(len(self.__banco)):
      soma += self.__banco[i].saldo
      return print(soma)
  def transferir(self, valor, destino):
    if valor <= self.__saldo:
      self.__saldo -= valor
      destino.saldo += valor
      return True
    return False
  def depositar(self, valor):
    self.saldo += valor
  def sacar(self, valor):
    if valor <= self.saldo:
      self.saldo -= valor
      return True
    return False
  def adicionar_conta(self, Conta):
    self.__banco.append(Conta_Corrente(Conta[0], Conta[1], Conta[2))
  def remover_conta(self, conta):
    for i in range(len(self.__banco)):
      if self.__banco[i] == conta:
        self.__banco[i]
        return True
      return False

#Parte principal
Nubank = Banco()
for i in range (2):
  Numero, Saldo, Nome_Bancario = input('Insira o numero, saldo e nome da conta a ser adicionada: ').split('')
  Numero, Saldo, Nome_Bancario = str(Numero), int(Saldo), str(Nome_Bancario)
  Nubank.append(Conta_Corrente(Numero, Saldo, Nome_Bancario))
comando = 0
while comando != 4:
  print('1.Depositar\n2.Sacar\n3.Saldo\n4.Sair')
  comando = int(input('Insira um dos numeros imprimidos: '))
  if comando == 1:
    Numero_Bancario = str(input('Insira o numero de uma conta: '))
    for i in range(len(Nubank)):
      if Numero_Bancario == Nubank[i].numero:
        Dinheiro = input('Insira o valor a ser depositado: ')
        Nubank[i].depositar(Dinheiro)
  elif comando == 2:
    Numero_Bancario = str(input('Insira o numero de uma conta: '))
    for i in range(len(Nubank)):
      if Numero_Bancario == Nubank[i].numero:
         Dinheiro = input('Insira o valor a ser sacado: ')
         Confirma = Nubank[i].sacar(Dinheiro)
         if Confirma == True:
          print('Realizada com sucesso')
         else:
          print('Deposito falho')
  elif comando == 3:
    Numero_Bancario = str(input('Insira o numero de uma conta: '))
    for i in range(len(Nubank)):
      if Numero_Bancario == Nubank[i].numero:
        print(Nubank[1].saldo)
  elif comando == 4:
    print('Operações encerradas')
    break
  else:
    print('Insira um comando valido.')
